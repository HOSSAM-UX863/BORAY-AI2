
workflows:
  android-workflow:
    name: Android Build
    environment:
      java: 17
    scripts:
      - cd android && chmod +x gradlew && ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk

