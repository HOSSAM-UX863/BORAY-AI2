workflows:
  android-workflow:
    name: Android Build
    environment:
      java: 17
    scripts:
      - cd $(find . -name "gradlew" -exec dirname {} \; | head -n 1) && chmod +x gradlew && ./gradlew assembleDebug
    artifacts:
      - "**/build/outputs/**/*.apk"
