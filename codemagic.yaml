workflows:
  android-workflow:
    name: Android Build

    environment:
      java: 17

    scripts:
      - name: Extract Project
        script: |
          unzip -o app.zip

      - name: Build APK
        script: |
          cd app
          gradle assembleDebug --no-daemon

    artifacts:
      - "app/**/build/outputs/**/*.apk"
